# Backend Environment Variables
# Skopiuj ten plik do .env i uzupełnij wartości

# Gemini API Key - wymagany do działania aplikacji
# Uzyskaj klucz API z: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI API Key - do embeddings (wymagany!)
OPENAI_API_KEY=twoj_openai_klucz_tutaj

# Port serwera backendu (opcjonalny, domyślnie 3001)
PORT=3001

# Środowisko uruchomienia (development/production)
# W trybie development wyświetlane są dodatkowe informacje o błędach
NODE_ENV=development

# Frontend Environment Variables (Vite)
# URL backendu API - używany przez frontend do komunikacji z backendem
# W trybie development zwykle http://localhost:3001
VITE_API_BASE_URL=http://localhost:3001

# Rate Limiting
# Okno czasowe w milisekundach (domyślnie 60000 = 1 minuta)
RATE_LIMIT_WINDOW_MS=60000

# Maksymalna liczba zapytań w oknie czasowym (domyślnie 10)
RATE_LIMIT_MAX=10

# Włącz/wyłącz rate limiting (true/false, domyślnie true)
# Ustaw na false, aby wyłączyć rate limiting w development
RATE_LIMIT_ENABLED=true

# Cache Configuration
# Maksymalna liczba wpisów w cache (domyślnie 100)
CACHE_MAX_SIZE=100

# Domyślny czas życia cache w milisekundach (domyślnie 86400000 = 24 godziny)
CACHE_DEFAULT_TTL=86400000

# Share Configuration
# Czas życia udostępnień w milisekundach (domyślnie 604800000 = 7 dni)
SHARE_TTL=604800000

# RAG Configuration
# Liczba chunków do zwrócenia w wyszukiwaniu (domyślnie 20)
RAG_TOP_K=20

# Model OpenAI embeddings (domyślnie text-embedding-3-small)
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Maksymalna długość tekstu dla embeddings w znakach (domyślnie 6000)
RAG_MAX_EMBEDDING_LENGTH=6000

# Rozmiar batch dla embeddings OpenAI (domyślnie 100)
RAG_EMBEDDING_BATCH_SIZE=100

# Rozmiar batch dla embeddings Gemini (domyślnie 10)
RAG_GEMINI_BATCH_SIZE=10

# Maksymalna długość snippet w cytowaniach w znakach (domyślnie 1000)
RAG_MAX_CITATION_LENGTH=1000

# Próg dla ostatniego zdania w cytowaniu w znakach (domyślnie 800)
RAG_CITATION_THRESHOLD=800

# Długość klucza dla deduplikacji cytowań w znakach (domyślnie 100)
RAG_CITATION_KEY_LENGTH=100
